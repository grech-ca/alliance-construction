export enum Floors {
  One = 1,
  Two = 2,
}

export enum ProjectType {
  Exemplary = 'exemplary',
  Individual = 'individual',
}

export enum Area {
  Small = 'small',
  Medium = 'medium',
  Large = 'large',
}
